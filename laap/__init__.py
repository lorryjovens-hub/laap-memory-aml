"""LAAP Memory — AML submission package.

This is a **minimal, self-contained slice** of the LAAP project containing only
the Agent Memory Leaderboard (AML) memory service. It deliberately does not
import the rest of the LAAP framework so that the evaluation contract can be
reproduced with two dependencies.
"""

__version__ = "1.0.2"
